spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE    
    
    
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: connect@insigniacs.in
    password: xznamktuoohrdtzb
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true   